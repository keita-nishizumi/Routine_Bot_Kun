<!DOCTYPE html>
<html>
  <head>
    <title>RoutineBotKun</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="http://yui.yahooapis.com/3.18.1/build/cssreset/cssreset-min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="grid-6"><a href="/">るーティンBotくん</a></h1>
        <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
            <%= link_to "New", new_routine_path, class: "post" %>
            <%= link_to "#{current_user.nickname}", routines_path, class: "post" %>
          </div>
        <% else %>
          <div class="user_nav grid-6">
            <%= link_to "Sign Up", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer>
     <p>Copyright Routine Bot Kun 2020.</p>
    </footer>
  </body>
</html>
