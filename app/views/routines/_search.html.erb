<%= form_with(url: path, local: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "Search #{placeholder_text}", class: "search-input"%>
  <%= form.submit "Search", class: "search-btn" %>
<% end %>